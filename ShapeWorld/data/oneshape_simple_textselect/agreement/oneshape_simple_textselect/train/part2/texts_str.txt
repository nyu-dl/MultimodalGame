there is a magenta triangle .  || there is an ellipse .   || there is a gray pentagon .  || there is a cyan square .  || there is a cyan triangle .  || there is a yellow cross .  || there is a gray cross .  || there is a green shape .  || there is a magenta triangle .  || there is a gray square . 
there is a gray rectangle .  || there is a yellow shape .  || there is a red triangle .  || there is a magenta triangle .  || there is a magenta shape .  || there is a blue pentagon .  || there is a magenta triangle .  || there is a blue shape .  || there is a cyan shape .  || there is a gray cross . 
there is a cross .   || there is a cyan triangle .  || there is a magenta pentagon .  || there is a green shape .  || there is a cyan shape .  || there is a red shape .  || there is a square .   || there is a magenta pentagon .  || there is a blue shape .  || there is a green pentagon . 
there is a blue pentagon .  || there is a gray rectangle .  || there is a blue shape .  || there is a blue pentagon .  || there is a magenta shape .  || there is a green shape .  || there is a cyan triangle .  || there is a gray cross .  || there is a cyan triangle .  || there is a gray cross . 
there is a green ellipse .  || there is a magenta shape .  || there is a red ellipse .  || there is a semicircle .   || there is an ellipse .   || there is a magenta triangle .  || there is a green shape .  || there is a gray square .  || there is a yellow shape .  || there is a cyan circle . 
there is a cross .   || there is a yellow square .  || there is a circle .   || there is a green shape .  || there is a blue square .  || there is a magenta rectangle .  || there is a magenta shape .  || there is a magenta shape .  || there is a semicircle .   || there is a magenta triangle . 
there is a magenta shape .  || there is a green shape .  || there is a pentagon .   || there is a yellow circle .  || there is a circle .   || there is a yellow square .  || there is a blue shape .  || there is a circle .   || there is a magenta semicircle .  || there is a blue square . 
there is a green square .  || there is a blue semicircle .  || there is a magenta semicircle .  || there is a magenta circle .  || there is a square .   || there is a green shape .  || there is a green semicircle .  || there is a blue shape .  || there is a blue shape .  || there is a cyan triangle . 
there is a yellow shape .  || there is a blue pentagon .  || there is a green shape .  || there is a pentagon .   || there is a red shape .  || there is a red ellipse .  || there is a yellow semicircle .  || there is a gray circle .  || there is a blue semicircle .  || there is a cross .  
there is a green pentagon .  || there is a magenta shape .  || there is a magenta pentagon .  || there is a magenta shape .  || there is a gray shape .  || there is a magenta triangle .  || there is a yellow shape .  || there is a semicircle .   || there is a yellow shape .  || there is a red triangle . 
there is a pentagon .   || there is a gray cross .  || there is a magenta ellipse .  || there is a square .   || there is a red shape .  || there is a yellow shape .  || there is a cross .   || there is a gray pentagon .  || there is a cyan triangle .  || there is a magenta shape . 
there is a magenta pentagon .  || there is a red ellipse .  || there is a yellow pentagon .  || there is a circle .   || there is a yellow circle .  || there is a gray pentagon .  || there is a gray pentagon .  || there is a magenta shape .  || there is a gray rectangle .  || there is a blue square . 
there is a yellow ellipse .  || there is a magenta square .  || there is a yellow shape .  || there is a square .   || there is a magenta shape .  || there is a cyan circle .  || there is a cross .   || there is a green shape .  || there is a green shape .  || there is a blue semicircle . 
there is a gray pentagon .  || there is a magenta shape .  || there is a cyan triangle .  || there is a green shape .  || there is a yellow semicircle .  || there is a yellow square .  || there is a red shape .  || there is a blue pentagon .  || there is a blue pentagon .  || there is a blue shape . 
there is a magenta pentagon .  || there is a square .   || there is a square .   || there is a green shape .  || there is a green cross .  || there is a red ellipse .  || there is a gray circle .  || there is a cyan cross .  || there is a cyan pentagon .  || there is a blue cross . 
there is a square .   || there is a pentagon .   || there is a pentagon .   || there is a blue pentagon .  || there is a magenta shape .  || there is a green cross .  || there is a blue shape .  || there is a green circle .  || there is an ellipse .   || there is a gray shape . 
there is a gray shape .  || there is a yellow cross .  || there is a red pentagon .  || there is a magenta pentagon .  || there is a cyan cross .  || there is a blue pentagon .  || there is a green circle .  || there is a green shape .  || there is a magenta pentagon .  || there is a green shape . 
there is a blue shape .  || there is an ellipse .   || there is a red shape .  || there is a cyan square .  || there is a magenta semicircle .  || there is a cross .   || there is a cross .   || there is a pentagon .   || there is a square .   || there is a pentagon .  
there is a yellow semicircle .  || there is a gray pentagon .  || there is a magenta circle .  || there is a semicircle .   || there is a gray shape .  || there is a square .   || there is a blue triangle .  || there is a blue pentagon .  || there is a gray shape .  || there is a magenta circle . 
there is a magenta shape .  || there is a red pentagon .  || there is a gray rectangle .  || there is a blue pentagon .  || there is a yellow semicircle .  || there is a yellow ellipse .  || there is a gray shape .  || there is a blue shape .  || there is a green shape .  || there is a magenta shape . 
there is a blue shape .  || there is a blue pentagon .  || there is a gray circle .  || there is a blue square .  || there is a magenta shape .  || there is a yellow semicircle .  || there is a square .   || there is a pentagon .   || there is a square .   || there is a red ellipse . 
there is a gray shape .  || there is a cyan triangle .  || there is a circle .   || there is a gray square .  || there is a cross .   || there is a red pentagon .  || there is a semicircle .   || there is a green shape .  || there is a gray pentagon .  || there is a gray shape . 
there is a square .   || there is a cyan cross .  || there is a gray cross .  || there is a green shape .  || there is a green shape .  || there is a magenta triangle .  || there is a blue triangle .  || there is a magenta rectangle .  || there is a magenta pentagon .  || there is a gray pentagon . 
there is a gray cross .  || there is a magenta circle .  || there is a gray rectangle .  || there is a cyan triangle .  || there is a circle .   || there is a green circle .  || there is a blue semicircle .  || there is a green cross .  || there is a pentagon .   || there is a red shape . 
there is a magenta pentagon .  || there is a cyan shape .  || there is a cross .   || there is a gray cross .  || there is a gray pentagon .  || there is a gray cross .  || there is a cyan circle .  || there is a gray pentagon .  || there is a cyan square .  || there is a gray pentagon . 
there is a semicircle .   || there is a cross .   || there is a blue shape .  || there is a green shape .  || there is a square .   || there is a yellow shape .  || there is a magenta ellipse .  || there is a green cross .  || there is a green pentagon .  || there is a blue shape . 
there is a gray pentagon .  || there is a red pentagon .  || there is a green semicircle .  || there is a square .   || there is a gray shape .  || there is a green cross .  || there is a gray cross .  || there is a magenta semicircle .  || there is a circle .   || there is a blue shape . 
there is a gray cross .  || there is a magenta shape .  || there is a square .   || there is a blue shape .  || there is a cyan pentagon .  || there is a cross .   || there is a red shape .  || there is a green cross .  || there is a yellow pentagon .  || there is a green semicircle . 
there is a red pentagon .  || there is a circle .   || there is a green cross .  || there is a yellow pentagon .  || there is a green ellipse .  || there is a cyan triangle .  || there is a circle .   || there is a red shape .  || there is a magenta ellipse .  || there is a cross .  
there is a semicircle .   || there is a cyan pentagon .  || there is a magenta pentagon .  || there is a cyan shape .  || there is a red triangle .  || there is a green pentagon .  || there is a gray pentagon .  || there is a yellow square .  || there is an ellipse .   || there is a cyan triangle . 
there is a gray cross .  || there is a red shape .  || there is a cyan circle .  || there is a magenta rectangle .  || there is a red shape .  || there is a blue pentagon .  || there is a green shape .  || there is an ellipse .   || there is a magenta semicircle .  || there is a blue shape . 
there is a cyan circle .  || there is a semicircle .   || there is a green square .  || there is a magenta circle .  || there is a circle .   || there is a magenta pentagon .  || there is a green cross .  || there is a magenta shape .  || there is an ellipse .   || there is a cyan shape . 
there is a magenta triangle .  || there is a blue pentagon .  || there is a green pentagon .  || there is a red shape .  || there is a gray cross .  || there is a red triangle .  || there is a magenta pentagon .  || there is a magenta shape .  || there is a magenta rectangle .  || there is a cyan shape . 
there is a blue semicircle .  || there is an ellipse .   || there is a pentagon .   || there is a semicircle .   || there is a blue semicircle .  || there is a gray rectangle .  || there is a gray rectangle .  || there is a green shape .  || there is a cyan square .  || there is a cyan triangle . 
there is a cyan circle .  || there is a magenta shape .  || there is a magenta shape .  || there is a magenta triangle .  || there is a magenta semicircle .  || there is a red ellipse .  || there is a semicircle .   || there is a gray shape .  || there is a magenta rectangle .  || there is a magenta triangle . 
there is a cross .   || there is a yellow ellipse .  || there is a semicircle .   || there is a red pentagon .  || there is a yellow semicircle .  || there is a gray cross .  || there is a cyan circle .  || there is a green pentagon .  || there is a gray circle .  || there is a semicircle .  
there is a blue shape .  || there is a pentagon .   || there is an ellipse .   || there is a green circle .  || there is a green circle .  || there is a cyan triangle .  || there is a rectangle .   || there is a red shape .  || there is a circle .   || there is a square .  
there is a yellow pentagon .  || there is a blue semicircle .  || there is a gray rectangle .  || there is a square .   || there is a magenta shape .  || there is a pentagon .   || there is a magenta shape .  || there is a cross .   || there is a blue semicircle .  || there is a red triangle . 
there is a blue triangle .  || there is a yellow ellipse .  || there is a cyan shape .  || there is a green shape .  || there is a gray shape .  || there is a yellow semicircle .  || there is a gray pentagon .  || there is a blue semicircle .  || there is a yellow semicircle .  || there is a yellow semicircle . 
there is a magenta pentagon .  || there is a blue pentagon .  || there is a red ellipse .  || there is a gray pentagon .  || there is a green shape .  || there is a green pentagon .  || there is a gray shape .  || there is a red triangle .  || there is a magenta rectangle .  || there is a blue semicircle . 
there is a magenta ellipse .  || there is a magenta shape .  || there is a cyan pentagon .  || there is a blue cross .  || there is a cyan triangle .  || there is a green shape .  || there is a magenta shape .  || there is a yellow pentagon .  || there is a magenta rectangle .  || there is a cyan triangle . 
there is a magenta shape .  || there is a yellow cross .  || there is a gray pentagon .  || there is a circle .   || there is a green ellipse .  || there is a magenta circle .  || there is a circle .   || there is a cyan square .  || there is an ellipse .   || there is a red ellipse . 
there is a cross .   || there is a magenta shape .  || there is a red ellipse .  || there is a magenta semicircle .  || there is a magenta shape .  || there is a gray circle .  || there is a green circle .  || there is a magenta ellipse .  || there is a semicircle .   || there is a magenta triangle . 
there is a red shape .  || there is a square .   || there is a cyan pentagon .  || there is a green cross .  || there is a magenta shape .  || there is a green cross .  || there is a gray shape .  || there is a yellow square .  || there is a circle .   || there is a blue pentagon . 
there is a green square .  || there is a green ellipse .  || there is a magenta pentagon .  || there is a magenta triangle .  || there is a cyan circle .  || there is a blue triangle .  || there is a blue pentagon .  || there is a gray pentagon .  || there is a red shape .  || there is a gray rectangle . 
there is a green square .  || there is a gray rectangle .  || there is a cyan circle .  || there is a blue pentagon .  || there is a square .   || there is a green shape .  || there is a green ellipse .  || there is a yellow ellipse .  || there is a cross .   || there is a gray cross . 
there is a blue pentagon .  || there is a gray shape .  || there is a gray pentagon .  || there is a yellow circle .  || there is a pentagon .   || there is a green circle .  || there is a pentagon .   || there is a magenta rectangle .  || there is a semicircle .   || there is a cyan shape . 
there is a pentagon .   || there is a yellow ellipse .  || there is a green circle .  || there is a cross .   || there is a cyan triangle .  || there is a blue cross .  || there is a magenta triangle .  || there is a cyan cross .  || there is a yellow semicircle .  || there is a yellow semicircle . 
there is a blue shape .  || there is a green semicircle .  || there is a green shape .  || there is a red shape .  || there is a cyan cross .  || there is a gray rectangle .  || there is a blue semicircle .  || there is a pentagon .   || there is a magenta pentagon .  || there is a gray rectangle . 
there is a yellow square .  || there is a yellow ellipse .  || there is a green pentagon .  || there is a square .   || there is a semicircle .   || there is a cyan square .  || there is a blue pentagon .  || there is a gray shape .  || there is a green shape .  || there is a magenta shape . 
there is a green shape .  || there is a red shape .  || there is a cross .   || there is a blue shape .  || there is a gray pentagon .  || there is a magenta semicircle .  || there is a blue pentagon .  || there is a green pentagon .  || there is a magenta triangle .  || there is a magenta circle . 
there is a gray pentagon .  || there is a yellow shape .  || there is a magenta circle .  || there is a red shape .  || there is a cross .   || there is a green square .  || there is a blue shape .  || there is a green semicircle .  || there is a green pentagon .  || there is a green pentagon . 
there is a gray shape .  || there is a yellow ellipse .  || there is a green cross .  || there is a magenta square .  || there is a yellow square .  || there is a circle .   || there is a yellow circle .  || there is a blue shape .  || there is a circle .   || there is a green shape . 
there is a cyan triangle .  || there is a magenta square .  || there is a blue semicircle .  || there is a yellow square .  || there is a cross .   || there is a rectangle .   || there is a blue cross .  || there is a red pentagon .  || there is a gray rectangle .  || there is a cyan pentagon . 
there is a green ellipse .  || there is a green shape .  || there is a green shape .  || there is a pentagon .   || there is a green pentagon .  || there is a red pentagon .  || there is a magenta square .  || there is a circle .   || there is a cyan circle .  || there is a yellow shape . 
there is a red shape .  || there is a circle .   || there is a gray pentagon .  || there is a cross .   || there is a blue shape .  || there is a blue shape .  || there is a yellow shape .  || there is a yellow pentagon .  || there is a cross .   || there is a magenta triangle . 
there is a green semicircle .  || there is a red pentagon .  || there is a blue pentagon .  || there is a magenta rectangle .  || there is a red triangle .  || there is a pentagon .   || there is a square .   || there is a yellow shape .  || there is a red shape .  || there is a green shape . 
there is a blue pentagon .  || there is a red shape .  || there is a cyan circle .  || there is a red shape .  || there is a green cross .  || there is a cyan pentagon .  || there is a cyan triangle .  || there is a blue pentagon .  || there is a gray rectangle .  || there is a circle .  
there is a cyan shape .  || there is a blue shape .  || there is a cross .   || there is a magenta triangle .  || there is a gray shape .  || there is a green shape .  || there is a magenta ellipse .  || there is a gray circle .  || there is a blue shape .  || there is a blue triangle . 
there is a yellow pentagon .  || there is a green circle .  || there is a square .   || there is a magenta semicircle .  || there is a cross .   || there is a blue shape .  || there is a cyan circle .  || there is a blue pentagon .  || there is a gray rectangle .  || there is a cyan triangle . 
there is a cyan triangle .  || there is a blue shape .  || there is a gray cross .  || there is a square .   || there is a circle .   || there is a cross .   || there is a semicircle .   || there is a blue semicircle .  || there is a yellow ellipse .  || there is a cyan cross . 
there is a magenta triangle .  || there is a green circle .  || there is a blue pentagon .  || there is a magenta shape .  || there is a blue square .  || there is a circle .   || there is a green square .  || there is a blue pentagon .  || there is an ellipse .   || there is a cyan triangle . 
there is a magenta shape .  || there is a magenta rectangle .  || there is a cyan shape .  || there is a yellow semicircle .  || there is a red pentagon .  || there is a blue triangle .  || there is a cyan shape .  || there is a green circle .  || there is a pentagon .   || there is a magenta pentagon . 
there is a gray shape .  || there is a green circle .  || there is a cyan triangle .  || there is a magenta triangle .  || there is a gray circle .  || there is a cross .   || there is a green shape .  || there is a blue square .  || there is a blue shape .  || there is a green shape . 
there is a magenta shape .  || there is a blue triangle .  || there is a magenta square .  || there is a gray shape .  || there is a magenta triangle .  || there is a yellow square .  || there is a cyan triangle .  || there is a cyan cross .  || there is a magenta shape .  || there is a magenta shape . 
there is a blue triangle .  || there is a red shape .  || there is a blue cross .  || there is an ellipse .   || there is a pentagon .   || there is a magenta shape .  || there is a gray cross .  || there is a yellow semicircle .  || there is a yellow semicircle .  || there is a blue semicircle . 
there is a cyan triangle .  || there is a cyan shape .  || there is a magenta ellipse .  || there is a yellow semicircle .  || there is a green shape .  || there is a cross .   || there is a gray cross .  || there is a cyan shape .  || there is a cyan triangle .  || there is a square .  
there is a magenta rectangle .  || there is a blue pentagon .  || there is a blue shape .  || there is a gray cross .  || there is an ellipse .   || there is a yellow pentagon .  || there is a green semicircle .  || there is a circle .   || there is a green square .  || there is a cross .  
there is a blue shape .  || there is a green square .  || there is a blue square .  || there is a pentagon .   || there is a pentagon .   || there is a blue pentagon .  || there is a green cross .  || there is a magenta shape .  || there is a magenta shape .  || there is a gray shape . 
there is a gray rectangle .  || there is a yellow cross .  || there is a red pentagon .  || there is a cyan cross .  || there is a blue shape .  || there is a gray rectangle .  || there is a green cross .  || there is an ellipse .   || there is a magenta ellipse .  || there is a blue square . 
there is a gray pentagon .  || there is a square .   || there is a magenta circle .  || there is a blue pentagon .  || there is a cyan pentagon .  || there is a blue shape .  || there is a green shape .  || there is a green cross .  || there is a rectangle .   || there is a gray shape . 
there is a magenta triangle .  || there is a magenta rectangle .  || there is a gray circle .  || there is a yellow shape .  || there is a yellow pentagon .  || there is a yellow pentagon .  || there is a blue triangle .  || there is a magenta triangle .  || there is a square .   || there is a blue square . 
there is a red ellipse .  || there is a green circle .  || there is an ellipse .   || there is a magenta shape .  || there is a blue shape .  || there is a green shape .  || there is a green shape .  || there is a cross .   || there is a gray pentagon .  || there is a cyan circle . 
there is a gray shape .  || there is a green semicircle .  || there is a gray pentagon .  || there is a cyan triangle .  || there is a blue semicircle .  || there is a green shape .  || there is a magenta pentagon .  || there is a green semicircle .  || there is a magenta shape .  || there is a cross .  
there is a green semicircle .  || there is a red triangle .  || there is a gray circle .  || there is a green circle .  || there is a magenta pentagon .  || there is a blue shape .  || there is a cyan shape .  || there is a green shape .  || there is a circle .   || there is a gray cross . 
there is a circle .   || there is a blue pentagon .  || there is a gray shape .  || there is a green square .  || there is a square .   || there is a magenta triangle .  || there is a cyan triangle .  || there is a yellow semicircle .  || there is a blue shape .  || there is a green cross . 
there is a yellow shape .  || there is a pentagon .   || there is a yellow shape .  || there is a cyan cross .  || there is a square .   || there is a gray shape .  || there is a gray pentagon .  || there is a yellow circle .  || there is a yellow circle .  || there is a magenta triangle . 
there is a green shape .  || there is a green cross .  || there is a gray square .  || there is a red shape .  || there is a cross .   || there is a gray cross .  || there is a green shape .  || there is a blue shape .  || there is a red shape .  || there is a blue square . 
there is an ellipse .   || there is a blue triangle .  || there is a cyan pentagon .  || there is an ellipse .   || there is a magenta shape .  || there is a green pentagon .  || there is a gray cross .  || there is a yellow semicircle .  || there is a magenta triangle .  || there is a cyan triangle . 
there is a blue square .  || there is a blue shape .  || there is a green shape .  || there is a cyan square .  || there is a red pentagon .  || there is a gray square .  || there is a green shape .  || there is a cyan circle .  || there is a red shape .  || there is a magenta ellipse . 
there is a gray pentagon .  || there is a gray shape .  || there is a gray shape .  || there is a rectangle .   || there is a yellow semicircle .  || there is a gray cross .  || there is a yellow square .  || there is a cyan pentagon .  || there is a cyan triangle .  || there is a blue shape . 
there is a yellow circle .  || there is a green cross .  || there is a yellow shape .  || there is a yellow pentagon .  || there is a red ellipse .  || there is a yellow ellipse .  || there is a yellow ellipse .  || there is a cyan shape .  || there is a red pentagon .  || there is a semicircle .  
there is a yellow pentagon .  || there is a gray rectangle .  || there is a red pentagon .  || there is a green pentagon .  || there is a blue pentagon .  || there is a blue semicircle .  || there is a circle .   || there is a red triangle .  || there is a blue square .  || there is a blue semicircle . 
there is a blue semicircle .  || there is an ellipse .   || there is a magenta pentagon .  || there is a red shape .  || there is a green circle .  || there is a magenta square .  || there is a gray shape .  || there is a green shape .  || there is a cyan triangle .  || there is a green shape . 
there is a magenta pentagon .  || there is a green square .  || there is a green cross .  || there is a gray square .  || there is a blue shape .  || there is a red shape .  || there is a semicircle .   || there is a blue pentagon .  || there is a cyan cross .  || there is a square .  
there is a gray rectangle .  || there is a cyan shape .  || there is a gray circle .  || there is a magenta pentagon .  || there is a magenta shape .  || there is a cross .   || there is a square .   || there is a rectangle .   || there is a circle .   || there is a yellow shape . 
there is a red triangle .  || there is a gray pentagon .  || there is a yellow shape .  || there is a yellow cross .  || there is a gray pentagon .  || there is a pentagon .   || there is a blue shape .  || there is a blue square .  || there is a magenta square .  || there is a gray pentagon . 
there is a yellow shape .  || there is a green shape .  || there is a red shape .  || there is a magenta shape .  || there is a green pentagon .  || there is a blue shape .  || there is a gray cross .  || there is a green circle .  || there is a red ellipse .  || there is an ellipse .  
there is a square .   || there is a circle .   || there is a cyan cross .  || there is a green ellipse .  || there is a yellow semicircle .  || there is a cyan triangle .  || there is a red triangle .  || there is a yellow shape .  || there is a gray shape .  || there is a magenta shape . 
there is a gray shape .  || there is a cross .   || there is a square .   || there is a cyan triangle .  || there is a magenta ellipse .  || there is a green shape .  || there is a blue semicircle .  || there is a yellow cross .  || there is a blue pentagon .  || there is a square .  
there is a gray rectangle .  || there is a green pentagon .  || there is a blue shape .  || there is a cross .   || there is a circle .   || there is a magenta square .  || there is a cross .   || there is a green shape .  || there is a red shape .  || there is a yellow circle . 
there is a yellow shape .  || there is a blue shape .  || there is a yellow shape .  || there is a cyan cross .  || there is a gray pentagon .  || there is a gray rectangle .  || there is a cyan cross .  || there is a gray rectangle .  || there is a green pentagon .  || there is a red triangle . 
there is a red shape .  || there is an ellipse .   || there is a yellow circle .  || there is a yellow cross .  || there is a cyan cross .  || there is a gray shape .  || there is a cyan shape .  || there is a magenta triangle .  || there is a blue pentagon .  || there is a red ellipse . 
there is a gray rectangle .  || there is a pentagon .   || there is a yellow semicircle .  || there is a red pentagon .  || there is a gray pentagon .  || there is a square .   || there is a cyan shape .  || there is a blue shape .  || there is a magenta shape .  || there is a green square . 
there is a circle .   || there is a red shape .  || there is a blue shape .  || there is a gray shape .  || there is a green shape .  || there is a green circle .  || there is a green semicircle .  || there is a green cross .  || there is a semicircle .   || there is a blue pentagon . 
there is a blue shape .  || there is a blue triangle .  || there is a yellow square .  || there is a magenta shape .  || there is a yellow shape .  || there is a green shape .  || there is a red triangle .  || there is a gray cross .  || there is a cyan pentagon .  || there is a semicircle .  
there is a gray cross .  || there is a yellow pentagon .  || there is a gray pentagon .  || there is a green cross .  || there is a yellow square .  || there is a magenta square .  || there is a green circle .  || there is a magenta shape .  || there is a blue shape .  || there is a gray cross . 
there is a cross .   || there is a semicircle .   || there is a green shape .  || there is a green cross .  || there is a yellow ellipse .  || there is a cyan triangle .  || there is a blue shape .  || there is a cyan cross .  || there is a green shape .  || there is a blue cross . 
there is a cyan cross .  || there is a green ellipse .  || there is a blue triangle .  || there is a pentagon .   || there is a gray shape .  || there is a cross .   || there is a cyan square .  || there is a cyan pentagon .  || there is a magenta triangle .  || there is a yellow circle . 
there is a magenta rectangle .  || there is a cyan cross .  || there is a blue cross .  || there is a blue triangle .  || there is a pentagon .   || there is a gray square .  || there is a magenta triangle .  || there is a yellow shape .  || there is a cyan triangle .  || there is a magenta ellipse . 
