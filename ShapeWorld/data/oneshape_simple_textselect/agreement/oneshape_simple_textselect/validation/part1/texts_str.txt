there is a green triangle .  || there is a green shape .  || there is a red shape .  || there is a green shape .  || there is a triangle .   || there is a green triangle .  || there is a red square .  || there is a blue shape .  || there is a blue circle .  || there is a circle .  
there is a green triangle .  || there is a green shape .  || there is a blue circle .  || there is a red shape .  || there is a blue circle .  || there is a red square .  || there is a blue shape .  || there is a circle .   || there is a triangle .   || there is a square .  
there is a red shape .  || there is a red square .  || there is a blue circle .  || there is a green triangle .  || there is a red square .  || there is a circle .   || there is a green shape .  || there is a green triangle .  || there is a blue shape .  || there is a triangle .  
there is a red square .  || there is a green triangle .  || there is a triangle .   || there is a blue circle .  || there is a blue shape .  || there is a blue circle .  || there is a red square .  || there is a red square .  || there is a blue shape .  || there is a blue circle . 
there is a green triangle .  || there is a red square .  || there is a blue shape .  || there is a green shape .  || there is a red square .  || there is a red shape .  || there is a green shape .  || there is a circle .   || there is a red square .  || there is a triangle .  
there is a triangle .   || there is a blue circle .  || there is a green triangle .  || there is a blue circle .  || there is a green triangle .  || there is a red shape .  || there is a green shape .  || there is a green triangle .  || there is a square .   || there is a blue shape . 
there is a green triangle .  || there is a red square .  || there is a triangle .   || there is a blue circle .  || there is a red shape .  || there is a blue circle .  || there is a green triangle .  || there is a green shape .  || there is a red square .  || there is a green triangle . 
there is a blue shape .  || there is a green shape .  || there is a blue shape .  || there is a square .   || there is a red square .  || there is a red square .  || there is a triangle .   || there is a blue circle .  || there is a red square .  || there is a green triangle . 
there is a green triangle .  || there is a green triangle .  || there is a blue circle .  || there is a red square .  || there is a green shape .  || there is a blue circle .  || there is a blue circle .  || there is a green triangle .  || there is a blue shape .  || there is a blue circle . 
there is a blue shape .  || there is a blue circle .  || there is a circle .   || there is a red square .  || there is a green triangle .  || there is a blue shape .  || there is a red square .  || there is a square .   || there is a blue shape .  || there is a red shape . 
there is a blue circle .  || there is a circle .   || there is a red square .  || there is a blue shape .  || there is a green shape .  || there is a blue circle .  || there is a blue circle .  || there is a green shape .  || there is a green shape .  || there is a green triangle . 
there is a blue shape .  || there is a blue circle .  || there is a green shape .  || there is a green triangle .  || there is a red square .  || there is a blue shape .  || there is a green triangle .  || there is a blue circle .  || there is a square .   || there is a green triangle . 
there is a red shape .  || there is a blue circle .  || there is a green shape .  || there is a circle .   || there is a green shape .  || there is a green triangle .  || there is a circle .   || there is a red shape .  || there is a blue shape .  || there is a circle .  
there is a square .   || there is a square .   || there is a red square .  || there is a green shape .  || there is a green triangle .  || there is a green shape .  || there is a triangle .   || there is a red square .  || there is a triangle .   || there is a blue shape . 
there is a circle .   || there is a green triangle .  || there is a red shape .  || there is a triangle .   || there is a green shape .  || there is a red square .  || there is a blue shape .  || there is a blue circle .  || there is a green shape .  || there is a blue circle . 
there is a red shape .  || there is a blue circle .  || there is a green triangle .  || there is a blue shape .  || there is a triangle .   || there is a green triangle .  || there is a green shape .  || there is a green triangle .  || there is a red shape .  || there is a blue shape . 
there is a blue circle .  || there is a green shape .  || there is a blue circle .  || there is a green shape .  || there is a red square .  || there is a green triangle .  || there is a green shape .  || there is a blue circle .  || there is a triangle .   || there is a blue circle . 
there is a blue circle .  || there is a red square .  || there is a square .   || there is a blue shape .  || there is a blue circle .  || there is a red square .  || there is a triangle .   || there is a blue circle .  || there is a green shape .  || there is a blue shape . 
there is a blue circle .  || there is a green shape .  || there is a blue shape .  || there is a blue circle .  || there is a green shape .  || there is a circle .   || there is a square .   || there is a green triangle .  || there is a blue circle .  || there is a square .  
there is a red square .  || there is a blue circle .  || there is a triangle .   || there is a green shape .  || there is a blue circle .  || there is a blue circle .  || there is a red square .  || there is a green triangle .  || there is a red square .  || there is a blue circle . 
there is a blue circle .  || there is a green triangle .  || there is a circle .   || there is a green shape .  || there is a red square .  || there is a red square .  || there is a green shape .  || there is a blue circle .  || there is a blue shape .  || there is a red shape . 
there is a green shape .  || there is a green shape .  || there is a blue circle .  || there is a green triangle .  || there is a blue circle .  || there is a triangle .   || there is a blue circle .  || there is a green shape .  || there is a green triangle .  || there is a green triangle . 
there is a red shape .  || there is a circle .   || there is a circle .   || there is a green triangle .  || there is a blue shape .  || there is a blue circle .  || there is a blue shape .  || there is a green triangle .  || there is a green triangle .  || there is a green shape . 
there is a blue shape .  || there is a green triangle .  || there is a triangle .   || there is a green shape .  || there is a red square .  || there is a circle .   || there is a red square .  || there is a red square .  || there is a triangle .   || there is a green shape . 
there is a triangle .   || there is a triangle .   || there is a blue circle .  || there is a triangle .   || there is a green shape .  || there is a blue shape .  || there is a red square .  || there is a blue circle .  || there is a circle .   || there is a green shape . 
there is a triangle .   || there is a blue circle .  || there is a green triangle .  || there is a red shape .  || there is a green shape .  || there is a blue shape .  || there is a green triangle .  || there is a red square .  || there is a green triangle .  || there is a blue circle . 
there is a red square .  || there is a red shape .  || there is a triangle .   || there is a red square .  || there is a green triangle .  || there is a triangle .   || there is a red square .  || there is a green shape .  || there is a green shape .  || there is a blue circle . 
there is a green triangle .  || there is a green shape .  || there is a square .   || there is a red square .  || there is a green triangle .  || there is a red shape .  || there is a blue shape .  || there is a circle .   || there is a circle .   || there is a red square . 
there is a green triangle .  || there is a red square .  || there is a green triangle .  || there is a green triangle .  || there is a green triangle .  || there is a green shape .  || there is a blue circle .  || there is a red shape .  || there is a circle .   || there is a red square . 
there is a blue circle .  || there is a red square .  || there is a square .   || there is a green triangle .  || there is a blue circle .  || there is a green shape .  || there is a red square .  || there is a green shape .  || there is a blue circle .  || there is a red square . 
there is a red square .  || there is a green shape .  || there is a green triangle .  || there is a red square .  || there is a green triangle .  || there is a red square .  || there is a green shape .  || there is a green triangle .  || there is a green shape .  || there is a square .  
there is a blue circle .  || there is a red square .  || there is a red square .  || there is a green triangle .  || there is a blue shape .  || there is a square .   || there is a blue shape .  || there is a blue shape .  || there is a blue shape .  || there is a blue shape . 
there is a green triangle .  || there is a red square .  || there is a blue circle .  || there is a red square .  || there is a red square .  || there is a green triangle .  || there is a triangle .   || there is a blue shape .  || there is a green triangle .  || there is a green shape . 
there is a red shape .  || there is a green triangle .  || there is a green shape .  || there is a triangle .   || there is a green triangle .  || there is a blue circle .  || there is a green shape .  || there is a red square .  || there is a blue circle .  || there is a circle .  
there is a circle .   || there is a green triangle .  || there is a red shape .  || there is a green triangle .  || there is a blue shape .  || there is a green triangle .  || there is a red square .  || there is a blue circle .  || there is a triangle .   || there is a blue shape . 
there is a triangle .   || there is a red square .  || there is a green triangle .  || there is a red square .  || there is a red shape .  || there is a red square .  || there is a red square .  || there is a triangle .   || there is a blue circle .  || there is a green shape . 
there is a red square .  || there is a green shape .  || there is a red shape .  || there is a blue circle .  || there is a green shape .  || there is a green shape .  || there is a green triangle .  || there is a green shape .  || there is a blue circle .  || there is a blue shape . 
there is a blue circle .  || there is a blue circle .  || there is a red square .  || there is a square .   || there is a green triangle .  || there is a blue circle .  || there is a blue circle .  || there is a triangle .   || there is a blue shape .  || there is a green shape . 
there is a square .   || there is a green triangle .  || there is a blue circle .  || there is a blue circle .  || there is a red square .  || there is a blue circle .  || there is a blue circle .  || there is a circle .   || there is a green triangle .  || there is a red shape . 
there is a blue shape .  || there is a green shape .  || there is a triangle .   || there is a green shape .  || there is a circle .   || there is a triangle .   || there is a red shape .  || there is a green shape .  || there is a square .   || there is a green triangle . 
there is a blue shape .  || there is a circle .   || there is a triangle .   || there is a green shape .  || there is a green shape .  || there is a red square .  || there is a blue circle .  || there is a green triangle .  || there is a blue circle .  || there is a red shape . 
there is a blue circle .  || there is a triangle .   || there is a circle .   || there is a circle .   || there is a triangle .   || there is a green triangle .  || there is a green shape .  || there is a blue shape .  || there is a blue circle .  || there is a blue circle . 
there is a green triangle .  || there is a green triangle .  || there is a triangle .   || there is a red square .  || there is a blue shape .  || there is a square .   || there is a green shape .  || there is a red square .  || there is a red square .  || there is a green triangle . 
there is a green triangle .  || there is a circle .   || there is a blue circle .  || there is a red square .  || there is a red square .  || there is a triangle .   || there is a red square .  || there is a blue circle .  || there is a blue circle .  || there is a blue circle . 
there is a square .   || there is a green shape .  || there is a red shape .  || there is a green triangle .  || there is a circle .   || there is a blue circle .  || there is a blue circle .  || there is a blue shape .  || there is a red shape .  || there is a green triangle . 
there is a green shape .  || there is a blue circle .  || there is a green triangle .  || there is a green shape .  || there is a blue circle .  || there is a circle .   || there is a red shape .  || there is a red square .  || there is a blue shape .  || there is a square .  
there is a blue circle .  || there is a circle .   || there is a blue circle .  || there is a blue circle .  || there is a green triangle .  || there is a red square .  || there is a green shape .  || there is a blue circle .  || there is a red square .  || there is a blue shape . 
there is a green triangle .  || there is a red square .  || there is a circle .   || there is a blue shape .  || there is a green triangle .  || there is a blue circle .  || there is a red square .  || there is a red square .  || there is a square .   || there is a red square . 
there is a red square .  || there is a blue shape .  || there is a red square .  || there is a green triangle .  || there is a blue shape .  || there is a red shape .  || there is a triangle .   || there is a red shape .  || there is a blue circle .  || there is a green triangle . 
there is a square .   || there is a square .   || there is a red shape .  || there is a triangle .   || there is a triangle .   || there is a square .   || there is a blue circle .  || there is a blue shape .  || there is a green triangle .  || there is a triangle .  
there is a red square .  || there is a blue circle .  || there is a green shape .  || there is a blue circle .  || there is a green shape .  || there is a blue shape .  || there is a blue circle .  || there is a blue circle .  || there is a green triangle .  || there is a red square . 
there is a green triangle .  || there is a blue circle .  || there is a circle .   || there is a blue circle .  || there is a blue shape .  || there is a blue circle .  || there is a red square .  || there is a green shape .  || there is a blue circle .  || there is a circle .  
there is a blue shape .  || there is a green triangle .  || there is a green triangle .  || there is a green triangle .  || there is a blue shape .  || there is a blue shape .  || there is a blue shape .  || there is a red square .  || there is a triangle .   || there is a green triangle . 
there is a square .   || there is a green triangle .  || there is a red square .  || there is a square .   || there is a circle .   || there is a red shape .  || there is a blue circle .  || there is a green triangle .  || there is a green triangle .  || there is a green triangle . 
there is a blue circle .  || there is a green shape .  || there is a red square .  || there is a red shape .  || there is a red shape .  || there is a green triangle .  || there is a blue shape .  || there is a green triangle .  || there is a red square .  || there is a green triangle . 
there is a circle .   || there is a green triangle .  || there is a green shape .  || there is a green triangle .  || there is a blue circle .  || there is a red shape .  || there is a red shape .  || there is a blue shape .  || there is a blue circle .  || there is a green shape . 
there is a green triangle .  || there is a red square .  || there is a green shape .  || there is a green triangle .  || there is a red square .  || there is a green triangle .  || there is a triangle .   || there is a blue circle .  || there is a red square .  || there is a blue circle . 
there is a green triangle .  || there is a circle .   || there is a blue circle .  || there is a triangle .   || there is a green triangle .  || there is a red square .  || there is a square .   || there is a green triangle .  || there is a green triangle .  || there is a green triangle . 
there is a blue circle .  || there is a blue circle .  || there is a green triangle .  || there is a red shape .  || there is a square .   || there is a red shape .  || there is a red square .  || there is a blue shape .  || there is a green shape .  || there is a green triangle . 
there is a blue circle .  || there is a red square .  || there is a green triangle .  || there is a red square .  || there is a green shape .  || there is a triangle .   || there is a circle .   || there is a square .   || there is a triangle .   || there is a blue shape . 
there is a red square .  || there is a circle .   || there is a blue circle .  || there is a red shape .  || there is a blue circle .  || there is a green triangle .  || there is a green shape .  || there is a blue circle .  || there is a green triangle .  || there is a green triangle . 
there is a green shape .  || there is a green shape .  || there is a red square .  || there is a circle .   || there is a green triangle .  || there is a red square .  || there is a green shape .  || there is a green triangle .  || there is a blue circle .  || there is a red square . 
there is a triangle .   || there is a blue circle .  || there is a red shape .  || there is a red square .  || there is a circle .   || there is a red square .  || there is a red square .  || there is a blue shape .  || there is a red square .  || there is a blue circle . 
there is a square .   || there is a square .   || there is a green triangle .  || there is a blue circle .  || there is a red square .  || there is a blue shape .  || there is a green shape .  || there is a green triangle .  || there is a triangle .   || there is a blue circle . 
there is a blue shape .  || there is a green shape .  || there is a circle .   || there is a square .   || there is a blue circle .  || there is a blue circle .  || there is a triangle .   || there is a red square .  || there is a red square .  || there is a green triangle . 
there is a red square .  || there is a green shape .  || there is a red square .  || there is a red square .  || there is a blue circle .  || there is a triangle .   || there is a green triangle .  || there is a red shape .  || there is a red square .  || there is a green shape . 
there is a square .   || there is a triangle .   || there is a red square .  || there is a green shape .  || there is a green shape .  || there is a red square .  || there is a blue shape .  || there is a blue circle .  || there is a green shape .  || there is a green shape . 
there is a blue circle .  || there is a green triangle .  || there is a circle .   || there is a blue circle .  || there is a triangle .   || there is a green triangle .  || there is a green triangle .  || there is a blue circle .  || there is a red square .  || there is a square .  
there is a red square .  || there is a blue circle .  || there is a blue circle .  || there is a circle .   || there is a red shape .  || there is a blue circle .  || there is a circle .   || there is a square .   || there is a red shape .  || there is a blue circle . 
there is a blue shape .  || there is a blue circle .  || there is a blue circle .  || there is a red shape .  || there is a red shape .  || there is a circle .   || there is a blue circle .  || there is a blue circle .  || there is a blue circle .  || there is a green triangle . 
there is a blue circle .  || there is a green shape .  || there is a circle .   || there is a triangle .   || there is a green triangle .  || there is a green triangle .  || there is a square .   || there is a blue shape .  || there is a red shape .  || there is a triangle .  
there is a blue circle .  || there is a triangle .   || there is a blue shape .  || there is a blue circle .  || there is a circle .   || there is a red square .  || there is a red square .  || there is a circle .   || there is a green shape .  || there is a blue circle . 
there is a green shape .  || there is a red square .  || there is a green shape .  || there is a green shape .  || there is a red square .  || there is a green triangle .  || there is a red square .  || there is a red square .  || there is a red square .  || there is a circle .  
there is a blue shape .  || there is a blue shape .  || there is a red square .  || there is a green triangle .  || there is a blue circle .  || there is a triangle .   || there is a green triangle .  || there is a blue shape .  || there is a triangle .   || there is a green shape . 
there is a blue circle .  || there is a triangle .   || there is a red square .  || there is a green shape .  || there is a red square .  || there is a blue circle .  || there is a green triangle .  || there is a green shape .  || there is a red square .  || there is a green shape . 
there is a blue circle .  || there is a green triangle .  || there is a red square .  || there is a red square .  || there is a red shape .  || there is a blue shape .  || there is a green triangle .  || there is a blue circle .  || there is a green shape .  || there is a green triangle . 
there is a blue circle .  || there is a green shape .  || there is a blue circle .  || there is a red square .  || there is a circle .   || there is a triangle .   || there is a blue shape .  || there is a circle .   || there is a green shape .  || there is a circle .  
there is a blue circle .  || there is a green triangle .  || there is a red square .  || there is a red shape .  || there is a blue shape .  || there is a blue circle .  || there is a green shape .  || there is a square .   || there is a blue circle .  || there is a blue circle . 
there is a red square .  || there is a green shape .  || there is a red shape .  || there is a red square .  || there is a red square .  || there is a triangle .   || there is a circle .   || there is a green shape .  || there is a red square .  || there is a blue shape . 
there is a red square .  || there is a square .   || there is a green triangle .  || there is a blue shape .  || there is a green shape .  || there is a circle .   || there is a blue shape .  || there is a circle .   || there is a blue circle .  || there is a green shape . 
there is a triangle .   || there is a green shape .  || there is a red square .  || there is a green triangle .  || there is a blue shape .  || there is a blue shape .  || there is a blue shape .  || there is a red square .  || there is a red square .  || there is a green shape . 
there is a green triangle .  || there is a green triangle .  || there is a green shape .  || there is a blue circle .  || there is a green triangle .  || there is a green triangle .  || there is a red square .  || there is a blue shape .  || there is a green shape .  || there is a green triangle . 
there is a square .   || there is a circle .   || there is a blue shape .  || there is a red square .  || there is a red shape .  || there is a triangle .   || there is a green shape .  || there is a blue shape .  || there is a triangle .   || there is a blue circle . 
there is a circle .   || there is a green triangle .  || there is a red square .  || there is a green triangle .  || there is a red square .  || there is a blue shape .  || there is a green triangle .  || there is a green triangle .  || there is a blue circle .  || there is a blue shape . 
there is a blue circle .  || there is a circle .   || there is a green triangle .  || there is a green shape .  || there is a red square .  || there is a green shape .  || there is a triangle .   || there is a green triangle .  || there is a green triangle .  || there is a blue circle . 
there is a blue circle .  || there is a circle .   || there is a blue circle .  || there is a blue circle .  || there is a triangle .   || there is a blue shape .  || there is a green triangle .  || there is a blue circle .  || there is a blue shape .  || there is a triangle .  
there is a green triangle .  || there is a green shape .  || there is a red square .  || there is a red square .  || there is a green shape .  || there is a blue circle .  || there is a circle .   || there is a red shape .  || there is a green triangle .  || there is a red square . 
there is a triangle .   || there is a red square .  || there is a blue circle .  || there is a green shape .  || there is a circle .   || there is a square .   || there is a green shape .  || there is a circle .   || there is a green triangle .  || there is a green shape . 
there is a square .   || there is a green shape .  || there is a red square .  || there is a triangle .   || there is a triangle .   || there is a triangle .   || there is a blue circle .  || there is a blue shape .  || there is a blue shape .  || there is a blue circle . 
there is a triangle .   || there is a circle .   || there is a blue shape .  || there is a green shape .  || there is a green triangle .  || there is a red square .  || there is a blue shape .  || there is a green shape .  || there is a blue shape .  || there is a green triangle . 
there is a green shape .  || there is a blue circle .  || there is a blue shape .  || there is a red square .  || there is a blue shape .  || there is a triangle .   || there is a blue circle .  || there is a blue shape .  || there is a red square .  || there is a red square . 
there is a red square .  || there is a circle .   || there is a circle .   || there is a green shape .  || there is a triangle .   || there is a blue circle .  || there is a red shape .  || there is a triangle .   || there is a blue shape .  || there is a green triangle . 
there is a triangle .   || there is a circle .   || there is a red square .  || there is a blue shape .  || there is a circle .   || there is a blue circle .  || there is a triangle .   || there is a green shape .  || there is a circle .   || there is a blue circle . 
there is a red square .  || there is a blue circle .  || there is a triangle .   || there is a blue circle .  || there is a circle .   || there is a green shape .  || there is a green triangle .  || there is a blue circle .  || there is a blue circle .  || there is a blue circle . 
there is a green shape .  || there is a green shape .  || there is a blue shape .  || there is a red square .  || there is a blue circle .  || there is a red square .  || there is a blue shape .  || there is a blue shape .  || there is a green shape .  || there is a green triangle . 
there is a circle .   || there is a red square .  || there is a circle .   || there is a blue circle .  || there is a green triangle .  || there is a blue circle .  || there is a red shape .  || there is a triangle .   || there is a blue shape .  || there is a square .  
there is a blue shape .  || there is a blue circle .  || there is a blue shape .  || there is a green shape .  || there is a green triangle .  || there is a blue circle .  || there is a blue shape .  || there is a blue circle .  || there is a red shape .  || there is a red square . 
there is a green shape .  || there is a blue circle .  || there is a blue shape .  || there is a blue circle .  || there is a green triangle .  || there is a red square .  || there is a triangle .   || there is a blue circle .  || there is a green triangle .  || there is a red square . 
there is a red shape .  || there is a green triangle .  || there is a blue shape .  || there is a blue circle .  || there is a circle .   || there is a green shape .  || there is a circle .   || there is a red square .  || there is a triangle .   || there is a square .  
there is a triangle .   || there is a green shape .  || there is a blue circle .  || there is a blue circle .  || there is a green shape .  || there is a blue circle .  || there is a blue circle .  || there is a triangle .   || there is a green triangle .  || there is a red square . 
